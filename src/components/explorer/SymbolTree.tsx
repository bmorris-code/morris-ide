import React from "react";

const SymbolTree: React.FC = () => {
  return (
    <div className="p-3 text-sm text-gray-400">
      Symbol tree (functions, classes, hooks)
    </div>
  );
};

export default SymbolTree;